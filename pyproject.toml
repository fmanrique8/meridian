[project]
name = "meridian"
version = "0.1.0"
description = "Workspace metadata for the Meridian monorepo."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.black]
target-version = ["py312"]
line-length = 88
